package pl.wildlabs.foxapi.api.data;

import org.bukkit.Location;

public class HologramData extends pl.wildlabs.wildapi.functions.data.HologramData {
    public HologramData(String hologramName, String hologramText, int hologramTime, Location hologramLocation) {
        super(hologramName, hologramText, hologramTime, hologramLocation);
    }
}